import { makeStyles } from '@material-ui/core'

export default makeStyles(theme => ({
    root: {
        display: "flex",
        marginLeft: 15,
        marginTop: 20
    },

    animated: {
        width: 80,
        height: 80,
        borderRadius: 40,
        marginBottom: 15
    }
}))