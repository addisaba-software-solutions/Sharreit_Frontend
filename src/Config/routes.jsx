import { REPO_ADDRESS } from "./config";

export default {
  root: "/" + REPO_ADDRESS + "/",
  signIn: "/" + REPO_ADDRESS + "/signIn",
  category: "/" + REPO_ADDRESS + "/CategoryPage",
  registration: "/" + REPO_ADDRESS + "/Registration",
  addItem: "/" + REPO_ADDRESS + "/AddItem",
  singleItem: "/" + REPO_ADDRESS + "/SingleItem",
  AllItems: "/" + REPO_ADDRESS + "/AllItems",
};
